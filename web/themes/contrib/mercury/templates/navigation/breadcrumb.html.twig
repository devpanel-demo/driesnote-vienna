{#
/**
 * @file
 * Theme override for a breadcrumb trail.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 */
#}
{{ attach_library('mercury/breadcrumb') }}
{% if breadcrumb %}
  {{ include('@mercury/components/breadcrumb/breadcrumb.twig') }}
{% endif %}
